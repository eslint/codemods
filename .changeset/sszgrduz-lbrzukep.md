---
'@eslint/v8-to-v9-custom-rules': patch
---

Fix incorrect CLI command in README (`npx codemod@latest run` → `npx codemod@latest`) and add missing deprecated `context` method mappings to the migration reference table.

/* eslint-env browser */
const el = document.getElementById('app')

/* eslint-env node, browser */
function read() {
  return process.env.NODE_ENV
}

// Regular comment — must not be touched
const x = 1
